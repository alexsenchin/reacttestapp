import React, {Component} from 'react';import style from './Nav.module.css'import {NavLink} from "react-router-dom";const Nav = () => {    return (        <nav className={style.nav}>            <div><NavLink className={style.navItem} to="/profile" activeClassName={style.activeLink}>Profile</NavLink></div>            <div><NavLink className={style.navItem} to="/messages" activeClassName={style.activeLink}>Messages</NavLink></div>            <div><NavLink className={style.navItem} to="/news" activeClassName={style.activeLink}>News</NavLink></div>            <div><NavLink className={style.navItem} to="/music" activeClassName={style.activeLink}>Music</NavLink></div>            <p></p>            <div><NavLink className={style.navItem} to="/settings">Settings</NavLink></div>        </nav>    )}export default Nav;